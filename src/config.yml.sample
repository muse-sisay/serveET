---
db_name : serve_ET

bot:
  token : # telegram toke id generated using Bot Father
  admin_id : # telegram id of server's admin

proxmox:
  host : # ip or FQDN of proxmox server
  node : # proxmox node
  user : # user to authenticate as
  token: 
    name : 
    value : 

template_vm:
    user: # default user for vm
    password: # defalt password for vm
    vm_id: # start of vm id 
    template_vms: # list of template vm's
      - name : 
        vm_id : 
